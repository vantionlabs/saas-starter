{
  "name": "@vantion/tokens",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "exports": {
    "./package.json": "./package.json",
    "./tokens.css": "./src/tokens.css",
    "./*": {
      "development": "./src/*.ts",
      "types": "./src/*.ts",
      "default": "./build/src/*.js"
    }
  },
  "scripts": {
    "build:css": "bun build.ts",
    "check:css": "bun build.ts --check",
    "check": "tsc -b tsconfig.json",
    "test": "bun --bun vitest run",
    "build": "tsc -b tsconfig.json",
    "figma:script": "bun figma.ts",
    "figma:json": "bun figma.ts --json"
  },
  "devDependencies": {
    "vitest": "catalog:"
  },
  "dependencies": {
    "@effect/platform-bun": "catalog:",
    "effect": "catalog:"
  }
}
