/// <reference types="nativewind/types" />

// NativeWind's augmentation of React Native's props: without this reference,
// `className` on a `<View>` is a type error and the styles it carries are
// invisible to the compiler.
