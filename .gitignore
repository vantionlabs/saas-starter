node_modules/
build/
dist/
.output/
.tanstack/
.tsbuildinfo/
*.tsbuildinfo
coverage/
.expo/

.direnv/
result
result-*

.env
.env.local

# The agent layer is committed on purpose: the hooks, the phase commands and
# the method are half of what this repository is. Only what is personal or
# ephemeral is ignored.
.claude/settings.local.json
.claude/worktrees/
# Skill launchers fetch a platform binary on first use; it is not ours to commit.
.claude/skills/*/scripts/bin/
.context/
.zed/
.DS_Store

# Where uploads land without S3 credentials. Not durable, and not ours to keep.
.data/

# The marketing site's server bundle, built only to prerender its one page.
apps/brand/.ssr/

# playwright
e2e/.e2e-env.json
e2e/test-results/
e2e/playwright-report/
e2e/blob-report/
docs/workflow/STATE.md
docs/workflow/prototype/
